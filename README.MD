这是一个鸣潮2周年庆版本活动小团快跑的比赛预测程序，可以用来模拟比赛。

### 启动参数

#### 必须的参数
* `--group` 指定比赛的分组，具体分组信息可以查看config.json，值为正整数，会随着比赛的进行更新剩下的分组。

#### 非必须的参数
* `-n` 模拟的轮数，默认值为10000。
* `--round` 模拟的场次，可选1和2,1为上半场，2为下半场，默认值为1。
